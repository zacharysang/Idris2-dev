;; Minimal init.el for local emacs with idris2-lsp container

;; Add MELPA repository for package installation (contains lsp-mode)
(add-to-list 'package-archives '("melpa" . "https://melpa.org/packages/") t)

;;(require 'package)
  
