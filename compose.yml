# Usage: PROJECT_PATH=/path/to/idris2-lsp docker compose up
services:
  idris2-lsp:
    build: 
      context: ./idris2-lsp
      args:
        USER_ID: ${USER_ID:-1000}
        GROUP_ID: ${GROUP_ID:-1000}
    ports:
      - "3030:3030"
    volumes:
      - ${PROJECT_PATH:-/tmp/a}:/workspace 
